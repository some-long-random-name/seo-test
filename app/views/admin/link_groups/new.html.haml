%ol.breadcrumb
  %li= link_to 'Pages', admin_pages_path
  %li= link_to @page.title, admin_page_path(@page)
  %li.active new link group

%h2 New link group for page #{@page.title}

= render partial: 'form', locals: { page: @page, link_group: @link_group }
